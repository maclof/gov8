# Exact toolchain pin for the oracle. Every cargo command run from this
# directory uses this toolchain, independent of the repository default.
# Record: rustc 1.98.0 (88d9e12ae 2026-08-18), cargo 1.98.0.
[toolchain]
channel = "1.98.0"
targets = ["x86_64-pc-windows-msvc"]
components = ["rustfmt", "clippy"]
